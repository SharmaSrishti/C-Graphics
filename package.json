{
  "name": "package-name",
  "version": "1.0.0",
  "description": "package description",
  "scripts": {
   "start" : "node index.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "start-script": "^0.1.0"
  },
  "devDependencies": {}
}
